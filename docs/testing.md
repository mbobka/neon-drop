# Локальная проверка

Дата: 10.09.2026. Окружение: Windows, .NET SDK 10.0.401, .NET MAUI Android, JDK 21.0.8, Android SDK API 36.

## Автоматические проверки

`dotnet test tests/NeonTetris.Core.Tests`: **150 passed, 0 failed** (после удаления резерва — 147, см. проверку 11.09.2026).

Покрыты 7-bag, коллизии и границы, SRS для разных переходов, ghost/drop, удаление линий и очки, game over, lock delay и предел сбросов таймера, независимость от частоты кадров, снимки с отключённой reflection-сериализацией, некорректные снимки, нулевая толщина и несколько сгибов, безопасные границы панелей.

`dotnet build src/NeonTetris/NeonTetris.csproj -f net10.0-android` с путями SDK/JDK: **0 warnings, 0 errors**.

`scripts/run-android.ps1 -Serial emulator-5554`: сборка, установка подписанного Debug APK и запуск Activity завершились успешно (`Status: ok`). Скрипты также проверены парсером PowerShell; одновременные `-Serial` и `-Avd` отклоняются.

## Проверка интерфейса

| Сценарий | Результат |
| --- | --- |
| Pixel 7 AVD, Android 15, 1080×2400 / 420 dpi | Запуск, сенсорные кнопки, перемещение, сброс, пауза — работают. |
| Клавиатура эмулятора | Enter, стрелки, Space, P доставляются в игру; подтверждено Android logcat и изменением счёта. |
| Убийство процесса и холодный запуск | JSON снимка до/после совпадает; восстановлены счёт, 20 занятых клеток и пауза. |
| Pixel Fold AVD, Android 15, 2208×1840 / 420 dpi | Запуск на широком внутреннем экране, квадратные клетки, поле отдельно от управления. |
| Горизонтальная ориентация телефона | Поле слева, управление справа; партия сохраняется, поворот включает паузу. |
| Pixel Fold, HALF_OPENED / book | Получен реальный callback AndroidX, после JNI-преобразования распознана одна разделяющая область; поле целиком слева от сгиба. |
| Pixel Fold, HALF_OPENED / tabletop | При повороте граница сгиба стала горизонтальной; поле сверху, кнопки снизу, без пересечения шарнира. |
| Pixel Fold, CLOSED / внешний экран 1080×2092 | Приложение перешло на внешний экран, включило паузу и сохранило активную фигуру; устаревший сгиб очищен. |

У AVD `NeonDrop_Fold_API35` доступен датчик `hinge-angle0` и состояния `CLOSED=0`, `HALF_OPENED=1`, `OPENED=2`. AndroidX сообщает `HardwareFoldingFeature`, в том числе разделяющую линию `[1104,0,1104,1840]` при HALF_OPENED.

Скриншоты: [телефон](screenshots/phone.png), [горизонтально](screenshots/landscape.png), [book](screenshots/fold-book.png), [tabletop](screenshots/fold-tabletop.png), [внешний экран](screenshots/fold-closed.png).

В коротком горизонтальном окне вспомогательная панель «ДАЛЬШЕ» прокручивается; основные кнопки движения и сброса остаются на месте.

Физические телефоны/планшеты, OEM-внешние экраны, iOS и Windows target не тестировались. Приложение собирается именно для Android. Эмулятор не подтверждает особенности прошивок всех моделей из обзора.

## Крестовина, 11.09.2026

Дата: 11.09.2026. Окружение: macOS, .NET SDK 10.0.401, Android SDK API 35, эмулятор Pixel 8 (Android 15, 1080×2400 / 420 dpi).

`dotnet test tests/NeonTetris.Core.Tests`: **147 passed, 0 failed** — тестов стало меньше, потому что резерв удалён.

`dotnet build src/NeonTetris/NeonTetris.csproj -f net10.0-android`: **0 warnings, 0 errors**.

| Сценарий | Результат |
| --- | --- |
| Pixel 8 AVD, портрет | Крестовина 64 dp в нижнем правом углу, «СБРОСИТЬ» и «?» над ней, поле не перекрыто. «ИГРАТЬ», ←, ↻, ↓ и «СБРОСИТЬ» отработали тапами: счёт 0 → 4 (мягкое падение) → 14 (мгновенное) → 74. |
| Переключатель «Рука» | «Рука: левая» переносит крестовину и «СБРОСИТЬ» в левый угол; выбор сохраняется в Preferences. |
| Ландшафт телефона | Двуручная раскладка: слева ← → и ↓, справа крупный ↻ с «СБРОСИТЬ» и «?», поле по центру. Счёт менялся тапами по обоим кластерам; поворот сохраняет партию и включает паузу. |
| «?» | Открывает справку без упоминания резерва. |

Симулятор iPhone 16 Pro (iOS): сборка `net10.0-ios` для `iossimulator-arm64` без предупреждений, стартовый экран и одноручная крестовина отрисованы. Симулятор iPad Pro 13-inch: широкая раскладка с полем по центру и кластерами в нижних углах. Тапы в симулятор не отправлялись — проверена только раскладка.

Складные панели на этот раз не проверялись: под рукой был только AVD Pixel 8, foldable-раскладка подтверждена лишь кодом и прошлым прогоном 10.09.2026.
