using UIKit;

namespace NeonTetris;

public class Program
{
    // Точка входа приложения на iOS.
    private static void Main(string[] args) => UIApplication.Main(args, null, typeof(AppDelegate));
}
